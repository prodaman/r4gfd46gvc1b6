name: Remove M3U history

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  clean-history:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout completo
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Instalar git-filter-repo
        run: |
          pip install git-filter-repo

      - name: Eliminar archivos m3u del historial
        run: |
          git filter-repo --path-glob "*.m3u" --invert-paths --force

      - name: Restaurar remote
        run: |
          git remote add origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/prodaman/r4gfd46gvc1b6.git

      - name: Forzar subida
        run: |
          git push --force --all origin
          git push --force --tags origin
